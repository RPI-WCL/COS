package src.testing.benchmarks;

// Import declarations generated by the SALSA compiler, do not modify.
import java.io.IOException;
import java.util.Vector;

import java.lang.reflect.Method;
import java.lang.reflect.Modifier;
import java.lang.reflect.InvocationTargetException;

import salsa.language.Actor;
import salsa.language.ActorReference;
import salsa.language.Message;
import salsa.language.RunTime;
import salsa.language.ServiceFactory;
import gc.WeakReference;
import salsa.language.Token;
import salsa.language.exceptions.*;
import salsa.language.exceptions.CurrentContinuationException;

import salsa.language.UniversalActor;

import salsa.naming.UAN;
import salsa.naming.UAL;
import salsa.naming.MalformedUALException;
import salsa.naming.MalformedUANException;

import salsa.resources.SystemService;

import salsa.resources.ActorService;

// End SALSA compiler generated import delcarations.

import java.io.FileInputStream;
import java.util.Vector;
import java.util.StringTokenizer;

public class Hypercube extends UniversalActor  {
	public static void main(String args[]) {
		UAN uan = null;
		UAL ual = null;
		if (System.getProperty("uan") != null) {
			uan = new UAN( System.getProperty("uan") );
			ServiceFactory.getTheater();
			RunTime.receivedUniversalActor();
		}
		if (System.getProperty("ual") != null) {
			ual = new UAL( System.getProperty("ual") );

			if (uan == null) {
				System.err.println("Actor Creation Error:");
				System.err.println("	uan: " + uan);
				System.err.println("	ual: " + ual);
				System.err.println("	Identifier: " + System.getProperty("identifier"));
				System.err.println("	Cannot specify an actor to have a ual at runtime without a uan.");
				System.err.println("	To give an actor a specific ual at runtime, use the identifier system property.");
				System.exit(0);
			}
			RunTime.receivedUniversalActor();
		}
		if (System.getProperty("identifier") != null) {
			if (ual != null) {
				System.err.println("Actor Creation Error:");
				System.err.println("	uan: " + uan);
				System.err.println("	ual: " + ual);
				System.err.println("	Identifier: " + System.getProperty("identifier"));
				System.err.println("	Cannot specify an identifier and a ual with system properties when creating an actor.");
				System.exit(0);
			}
			ual = new UAL( ServiceFactory.getTheater().getLocation() + System.getProperty("identifier"));
		}
		RunTime.receivedMessage();
		Hypercube instance = (Hypercube)new Hypercube(uan, ual,null).construct();
		gc.WeakReference instanceRef=new gc.WeakReference(uan,ual);
		{
			Object[] _arguments = { args };

			//preAct() for local actor creation
			//act() for remote actor creation
			if (ual != null && !ual.getLocation().equals(ServiceFactory.getTheater().getLocation())) {instance.send( new Message(instanceRef, instanceRef, "act", _arguments, false) );}
			else {instance.send( new Message(instanceRef, instanceRef, "preAct", _arguments, false) );}
		}
		RunTime.finishedProcessingMessage();
	}

	public static ActorReference getReferenceByName(UAN uan)	{ return new Hypercube(false, uan); }
	public static ActorReference getReferenceByName(String uan)	{ return Hypercube.getReferenceByName(new UAN(uan)); }
	public static ActorReference getReferenceByLocation(UAL ual)	{ return new Hypercube(false, ual); }

	public static ActorReference getReferenceByLocation(String ual)	{ return Hypercube.getReferenceByLocation(new UAL(ual)); }
	public Hypercube(boolean o, UAN __uan)	{ super(false,__uan); }
	public Hypercube(boolean o, UAL __ual)	{ super(false,__ual); }
	public Hypercube(UAN __uan,UniversalActor.State sourceActor)	{ this(__uan, null, sourceActor); }
	public Hypercube(UAL __ual,UniversalActor.State sourceActor)	{ this(null, __ual, sourceActor); }
	public Hypercube(UniversalActor.State sourceActor)		{ this(null, null, sourceActor);  }
	public Hypercube()		{  }
	public Hypercube(UAN __uan, UAL __ual, Object obj) {
		//decide the type of sourceActor
		//if obj is null, the actor must be the startup actor.
		//if obj is an actorReference, this actor is created by a remote actor

		if (obj instanceof UniversalActor.State || obj==null) {
			  UniversalActor.State sourceActor;
			  if (obj!=null) { sourceActor=(UniversalActor.State) obj;}
			  else {sourceActor=null;}

			  //remote creation message sent to a remote system service.
			  if (__ual != null && !__ual.getLocation().equals(ServiceFactory.getTheater().getLocation())) {
			    WeakReference sourceRef;
			    if (sourceActor!=null && sourceActor.getUAL() != null) {sourceRef = new WeakReference(sourceActor.getUAN(),sourceActor.getUAL());}
			    else {sourceRef = null;}
			    if (sourceActor != null) {
			      if (__uan != null) {sourceActor.getActorMemory().getForwardList().putReference(__uan);}
			      else if (__ual!=null) {sourceActor.getActorMemory().getForwardList().putReference(__ual);}

			      //update the source of this actor reference
			      setSource(sourceActor.getUAN(), sourceActor.getUAL());
			      activateGC();
			    }
			    createRemotely(__uan, __ual, "src.testing.benchmarks.Hypercube", sourceRef);
			  }

			  // local creation
			  else {
			    State state = new State(__uan, __ual);

			    //assume the reference is weak
			    muteGC();

			    //the source actor is  the startup actor
			    if (sourceActor == null) {
			      state.getActorMemory().getInverseList().putInverseReference("rmsp://me");
			    }

			    //the souce actor is a normal actor
			    else if (sourceActor instanceof UniversalActor.State) {

			      // this reference is part of garbage collection
			      activateGC();

			      //update the source of this actor reference
			      setSource(sourceActor.getUAN(), sourceActor.getUAL());

			      /* Garbage collection registration:
			       * register 'this reference' in sourceActor's forward list @
			       * register 'this reference' in the forward acquaintance's inverse list
			       */
			      String inverseRefString=null;
			      if (sourceActor.getUAN()!=null) {inverseRefString=sourceActor.getUAN().toString();}
			      else if (sourceActor.getUAL()!=null) {inverseRefString=sourceActor.getUAL().toString();}
			      if (__uan != null) {sourceActor.getActorMemory().getForwardList().putReference(__uan);}
			      else if (__ual != null) {sourceActor.getActorMemory().getForwardList().putReference(__ual);}
			      else {sourceActor.getActorMemory().getForwardList().putReference(state.getUAL());}

			      //put the inverse reference information in the actormemory
			      if (inverseRefString!=null) state.getActorMemory().getInverseList().putInverseReference(inverseRefString);
			    }
			    state.updateSelf(this);
			    ServiceFactory.getNaming().setEntry(state.getUAN(), state.getUAL(), state);
			    if (getUAN() != null) ServiceFactory.getNaming().update(state.getUAN(), state.getUAL());
			  }
		}

		//creation invoked by a remote message
		else if (obj instanceof ActorReference) {
			  ActorReference sourceRef= (ActorReference) obj;
			  State state = new State(__uan, __ual);
			  muteGC();
			  state.getActorMemory().getInverseList().putInverseReference("rmsp://me");
			  if (sourceRef.getUAN() != null) {state.getActorMemory().getInverseList().putInverseReference(sourceRef.getUAN());}
			  else if (sourceRef.getUAL() != null) {state.getActorMemory().getInverseList().putInverseReference(sourceRef.getUAL());}
			  state.updateSelf(this);
			  ServiceFactory.getNaming().setEntry(state.getUAN(), state.getUAL(),state);
			  if (getUAN() != null) ServiceFactory.getNaming().update(state.getUAN(), state.getUAL());
		}
	}

	public UniversalActor construct() {
		Object[] __arguments = { };
		this.send( new Message(this, this, "construct", __arguments, null, null) );
		return this;
	}

	public class State extends UniversalActor .State {
		public Hypercube self;
		public void updateSelf(ActorReference actorReference) {
			((Hypercube)actorReference).setUAL(getUAL());
			((Hypercube)actorReference).setUAN(getUAN());
			self = new Hypercube(false,getUAL());
			self.setUAN(getUAN());
			self.setUAL(getUAL());
			self.activateGC();
		}

		public void preAct(String[] arguments) {
			getActorMemory().getInverseList().removeInverseReference("rmsp://me",1);
			{
				Object[] __args={arguments};
				self.send( new Message(self,self, "act", __args, null,null,false) );
			}
		}

		public State() {
			this(null, null);
		}

		public State(UAN __uan, UAL __ual) {
			super(__uan, __ual);
			addClassName( "src.testing.benchmarks.Hypercube$State" );
			addMethodsForClasses();
		}

		public void construct() {}

		public void process(Message message) {
			Method[] matches = getMatches(message.getMethodName());
			Object returnValue = null;
			Exception exception = null;

			if (matches != null) {
				if (!message.getMethodName().equals("die")) {activateArgsGC(message);}
				for (int i = 0; i < matches.length; i++) {
					try {
						if (matches[i].getParameterTypes().length != message.getArguments().length) continue;
						returnValue = matches[i].invoke(this, message.getArguments());
					} catch (Exception e) {
						if (e.getCause() instanceof CurrentContinuationException) {
							sendGeneratedMessages();
							return;
						} else if (e instanceof InvocationTargetException) {
							sendGeneratedMessages();
							exception = (Exception)e.getCause();
							break;
						} else {
							continue;
						}
					}
					sendGeneratedMessages();
					currentMessage.resolveContinuations(returnValue);
					return;
				}
			}

			System.err.println("Message processing exception:");
			if (message.getSource() != null) {
				System.err.println("\tSent by: " + message.getSource().toString());
			} else System.err.println("\tSent by: unknown");
			System.err.println("\tReceived by actor: " + toString());
			System.err.println("\tMessage: " + message.toString());
			if (exception == null) {
				if (matches == null) {
					System.err.println("\tNo methods with the same name found.");
					return;
				}
				System.err.println("\tDid not match any of the following: ");
				for (int i = 0; i < matches.length; i++) {
					System.err.print("\t\tMethod: " + matches[i].getName() + "( ");
					Class[] parTypes = matches[i].getParameterTypes();
					for (int j = 0; j < parTypes.length; j++) {
						System.err.print(parTypes[j].getName() + " ");
					}
					System.err.println(")");
				}
			} else {
				System.err.println("\tThrew exception: " + exception);
				exception.printStackTrace();
			}
		}

		public void act(String[] arguments) {
			int size = 0;
			int length = 0;
			int width = 0;
			int depth = 0;
			int messageSendingDelay = 0;
			int messageSize = 0;
			int processingTime = 0;
			String id = "";
			String nameServer = "";
			Vector theaters = new Vector();
			try {
				if (arguments.length!=8) {{
					System.err.println("Usage: ");
					System.err.println("\tjava io.testing.benchmarks.Hypercube <naming/theater information file> "+"<length> <width> <depth> <messageSendingDelay(ms)> <messageSize(bytes)> <processingTime(ms)> <id>");
					System.exit(0);
				}
}				FileInputStream file;
				byte input[];
				try {
					file = new FileInputStream(arguments[0]);
					input = new byte[file.available()];
					file.read(input);
					StringTokenizer st = new StringTokenizer(new String(input), "\n");
					nameServer = st.nextToken();
					while (st.hasMoreTokens()) {
						theaters.add(st.nextToken());
					}
				}
				catch (Exception e) {
					System.err.println("Error reading naming/theater information file: "+e);
					System.exit(0);
				}

				length = new Integer(arguments[1]).intValue();
				width = new Integer(arguments[2]).intValue();
				depth = new Integer(arguments[3]).intValue();
				messageSendingDelay = new Integer(arguments[4]).intValue();
				messageSize = new Integer(arguments[5]).intValue();
				processingTime = new Integer(arguments[6]).intValue();
				id = arguments[7];
			}
			catch (Exception e) {
				System.err.println("Usage: ");
				System.err.println("\tjava io.testing.benchmarks.Hypercube <naming/theater information file> "+"<length> <width> <depth> <messageSendingDelay(ms)> <messageSize(bytes)> <processingTime(ms)>");
				System.exit(0);
			}

			size = length*width*depth;
			Token synchToken = new Token("synchToken");
			{
				Token token_2_0 = new Token();
				Token token_2_1 = new Token();
				Token token_2_2 = new Token();
				Token token_2_3 = new Token();
				Token token_2_4 = new Token();
				Token token_2_5 = new Token();
				Token token_2_6 = new Token();
				Token token_2_7 = new Token();
				Token token_2_8 = new Token();
				Token token_2_9 = new Token();
				Token token_2_10 = new Token();
				// standardOutput<-println("Creating a hypercube actor graph with: ")
				{
					Object _arguments[] = { "Creating a hypercube actor graph with: " };
					Message message = new Message( self, standardOutput, "println", _arguments, null, token_2_0 );
					__messages.add( message );
				}
				// standardOutput<-println("\tnodes in hypercube actor graph: "+size)
				{
					Object _arguments[] = { "\tnodes in hypercube actor graph: "+size };
					Message message = new Message( self, standardOutput, "println", _arguments, token_2_0, token_2_1 );
					__messages.add( message );
				}
				// standardOutput<-println("\tlength in hypercube actor graph: "+length)
				{
					Object _arguments[] = { "\tlength in hypercube actor graph: "+length };
					Message message = new Message( self, standardOutput, "println", _arguments, token_2_1, token_2_2 );
					__messages.add( message );
				}
				// standardOutput<-println("\twidth in hypercube actor graph: "+width)
				{
					Object _arguments[] = { "\twidth in hypercube actor graph: "+width };
					Message message = new Message( self, standardOutput, "println", _arguments, token_2_2, token_2_3 );
					__messages.add( message );
				}
				// standardOutput<-println("\tdepth in hypercube actor graph: "+depth)
				{
					Object _arguments[] = { "\tdepth in hypercube actor graph: "+depth };
					Message message = new Message( self, standardOutput, "println", _arguments, token_2_3, token_2_4 );
					__messages.add( message );
				}
				// standardOutput<-println("\tdelay between message sends: "+messageSendingDelay+"(ms)")
				{
					Object _arguments[] = { "\tdelay between message sends: "+messageSendingDelay+"(ms)" };
					Message message = new Message( self, standardOutput, "println", _arguments, token_2_4, token_2_5 );
					__messages.add( message );
				}
				// standardOutput<-println("\tsize of messages: "+messageSize+"(bytes)")
				{
					Object _arguments[] = { "\tsize of messages: "+messageSize+"(bytes)" };
					Message message = new Message( self, standardOutput, "println", _arguments, token_2_5, token_2_6 );
					__messages.add( message );
				}
				// standardOutput<-println("\tprocessing intensity of a message: "+processingTime+"(ms)")
				{
					Object _arguments[] = { "\tprocessing intensity of a message: "+processingTime+"(ms)" };
					Message message = new Message( self, standardOutput, "println", _arguments, token_2_6, token_2_7 );
					__messages.add( message );
				}
				// standardOutput<-println()
				{
					Object _arguments[] = {  };
					Message message = new Message( self, standardOutput, "println", _arguments, token_2_7, token_2_8 );
					__messages.add( message );
				}
				// standardOutput<-println("Using name server:")
				{
					Object _arguments[] = { "Using name server:" };
					Message message = new Message( self, standardOutput, "println", _arguments, token_2_8, token_2_9 );
					__messages.add( message );
				}
				// standardOutput<-println("\t"+nameServer)
				{
					Object _arguments[] = { "\t"+nameServer };
					Message message = new Message( self, standardOutput, "println", _arguments, token_2_9, token_2_10 );
					__messages.add( message );
				}
				// token synchToken = standardOutput<-println("Loading actor graph randomly on theater(s): ")
				{
					Object _arguments[] = { "Loading actor graph randomly on theater(s): " };
					Message message = new Message( self, standardOutput, "println", _arguments, token_2_10, synchToken );
					__messages.add( message );
				}
			}
			for (int i = 0; i<theaters.size(); i++){
				{
					// synchToken = standardOutput<-println("\t"+(String)theaters.get(i))
					Token synchToken_next = new Token("<-_next");
					{
						Object _arguments[] = { "\t"+(String)theaters.get(i) };
						Message message = new Message( self, standardOutput, "println", _arguments, null, synchToken_next );
						Object[] _propertyInfo = { synchToken };
						message.setProperty( "waitfor", _propertyInfo );
						__messages.add( message );
					}
					synchToken = synchToken_next;
				}
			}
			{
				// synchToken = standardOutput<-println()
				Token synchToken_next = new Token("<-_next");
				{
					Object _arguments[] = {  };
					Message message = new Message( self, standardOutput, "println", _arguments, null, synchToken_next );
					Object[] _propertyInfo = { synchToken };
					message.setProperty( "waitfor", _propertyInfo );
					__messages.add( message );
				}
				synchToken = synchToken_next;
			}
			Node[] nodes = new Node[size];
			for (int i = 0; i<size; i++){
				UAN uan = new UAN(nameServer+"benchmark/node_"+id+i);
				UAL ual = new UAL(theaters.get(i%theaters.size())+"benchmark/hypercube_node_"+id+i);
				nodes[i] = ((Node)new Node(uan, ual,this).construct(messageSendingDelay, messageSize, processingTime));
			}
			{
				Token token_2_0 = new Token();
				Token token_2_1 = new Token();
				Token token_2_2 = new Token();
				Token token_2_3 = new Token();
				// standardOutput<-println("Generating links...")
				{
					Object _arguments[] = { "Generating links..." };
					Message message = new Message( self, standardOutput, "println", _arguments, null, token_2_0 );
					Object[] _propertyInfo = { synchToken };
					message.setProperty( "waitfor", _propertyInfo );
					__messages.add( message );
				}
				// join block
				token_2_1.setJoinDirector();
				for (int i = 0; i<length; i++){
					for (int j = 0; j<width; j++){
						for (int k = 0; k<depth; k++){
							{
								Token token_6_0 = new Token();
								// join block
								token_6_0.setJoinDirector();
								if ((i+1)<length) {								{
									// nodes[length*width*k+length*j+i]<-addReference(nodes[length*width*k+length*j+i+1])
									{
										Object _arguments[] = { nodes[length*width*k+length*j+i+1] };
										Message message = new Message( self, nodes[length*width*k+length*j+i], "addReference", _arguments, token_2_0, token_6_0 );
										__messages.add( message );
									}
								}
}								if ((i-1)>=0) {								{
									// nodes[length*width*k+length*j+i]<-addReference(nodes[length*width*k+length*j+i-1])
									{
										Object _arguments[] = { nodes[length*width*k+length*j+i-1] };
										Message message = new Message( self, nodes[length*width*k+length*j+i], "addReference", _arguments, token_2_0, token_6_0 );
										__messages.add( message );
									}
								}
}								if ((j+1)<width) {								{
									// nodes[length*width*k+length*j+i]<-addReference(nodes[length*width*k+length*j+i+length])
									{
										Object _arguments[] = { nodes[length*width*k+length*j+i+length] };
										Message message = new Message( self, nodes[length*width*k+length*j+i], "addReference", _arguments, token_2_0, token_6_0 );
										__messages.add( message );
									}
								}
}								if ((j-1)>=0) {								{
									// nodes[length*width*k+length*j+i]<-addReference(nodes[length*width*k+length*j+i-length])
									{
										Object _arguments[] = { nodes[length*width*k+length*j+i-length] };
										Message message = new Message( self, nodes[length*width*k+length*j+i], "addReference", _arguments, token_2_0, token_6_0 );
										__messages.add( message );
									}
								}
}								if ((k+1)<depth) {								{
									// nodes[length*width*k+length*j+i]<-addReference(nodes[length*width*k+length*j+i+length*width])
									{
										Object _arguments[] = { nodes[length*width*k+length*j+i+length*width] };
										Message message = new Message( self, nodes[length*width*k+length*j+i], "addReference", _arguments, token_2_0, token_6_0 );
										__messages.add( message );
									}
								}
}								if ((k-1)>=0) {								{
									// nodes[length*width*k+length*j+i]<-addReference(nodes[length*width*k+length*j+i-length*width])
									{
										Object _arguments[] = { nodes[length*width*k+length*j+i-length*width] };
										Message message = new Message( self, nodes[length*width*k+length*j+i], "addReference", _arguments, token_2_0, token_6_0 );
										__messages.add( message );
									}
								}
}								addJoinToken(token_6_0);
								// standardOutput<-println("\tGenerated links for node_"+(length*width*k+length*j+i))
								{
									Object _arguments[] = { "\tGenerated links for node_"+(length*width*k+length*j+i) };
									Message message = new Message( self, standardOutput, "println", _arguments, token_6_0, token_2_1 );
									__messages.add( message );
								}
							}
						}
					}
				}
				addJoinToken(token_2_1);
				// standardOutput<-println("Starting computation...")
				{
					Object _arguments[] = { "Starting computation..." };
					Message message = new Message( self, standardOutput, "println", _arguments, token_2_1, token_2_2 );
					__messages.add( message );
				}
				// join block
				token_2_3.setJoinDirector();
				for (int i = 0; i<size; i++){
					{
						Token token_4_0 = new Token();
						// nodes[i]<-startSending()
						{
							Object _arguments[] = {  };
							Message message = new Message( self, nodes[i], "startSending", _arguments, token_2_2, token_4_0 );
							__messages.add( message );
						}
						// standardOutput<-println("\tnode_"+i+" is now processing.")
						{
							Object _arguments[] = { "\tnode_"+i+" is now processing." };
							Message message = new Message( self, standardOutput, "println", _arguments, token_4_0, token_2_3 );
							__messages.add( message );
						}
					}
				}
				addJoinToken(token_2_3);
				// standardOutput<-println("finished.")
				{
					Object _arguments[] = { "finished." };
					Message message = new Message( self, standardOutput, "println", _arguments, token_2_3, null );
					__messages.add( message );
				}
			}
		}
	}
}